##
var n := ReadInteger;
var (a, m) := (0, 0);
loop 1 do 
begin
  a := n div 100;
  m := n mod 100;  
  m := m * 10 + a;  
end;
m.Print; 

//!!!! ВНИМАНИЕ !!!!
//интересный разбор программы на пошаговому разбору работы программы! 
