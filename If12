##
var (a,b,c) := ReadlnReal3;
  var min: real;
  if (a<b) and (a<c) then min := a;
  if (b<a) and (b<c) then min := b;
  if (c<a) and (c<b) then min := c;
  min.Print;
/////////////////
##
uses PT4;
Task('if12');
var (a,b,c) := ReadReal3('Введите 3 числа:');
Print(Min(a,b,c));
