##
var (a,b) := ReadlnReal2;
  if (a > b) then 
  begin
    a.Println;
    b.Println;
  end;
  if (a < b) then 
  begin
    b.Println;
    a.Println;
  end;
///////////////////////
##
uses PT4;
Task('if8');
var (a,b) := ReadReal2('Введите 2 числа:');
var max := a > b ? a : b;
Print(max);
var min := a > b ? b : a;
Print(min);
